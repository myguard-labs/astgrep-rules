# MyGuard rule: https://github.com/myguard-labs/ast-grep-essentials | https://deb.myguard.nl
# CodeRabbit source repository: https://github.com/coderabbitai/ast-grep-essentials
# CodeRabbit source file: https://github.com/coderabbitai/ast-grep-essentials/blob/73120109bf45c284d0cd8a37bdd7082e80e92e87/rules/java/security/cookie-missing-httponly-java.yml
# Original author (Git): ESS-ENN
# License: Apache License 2.0 (https://www.apache.org/licenses/LICENSE-2.0)
# Modified by MyGuard: provenance metadata added; matcher behavior is unchanged.
id: cookie-missing-httponly-java
severity: warning
language: java
message: >-
  A cookie was detected without setting the 'HttpOnly' flag. The
  'HttpOnly' flag for cookies instructs the browser to forbid client-side
  scripts from reading the cookie. Set the 'HttpOnly' flag by calling
  'cookie.setHttpOnly(true);
note: >-
  [CWE-1004] Sensitive Cookie Without 'HttpOnly' Flag.
  [REFERENCES]
      - https://owasp.org/www-community/HttpOnly
ast-grep-essentials: true
rule:
  pattern: $RESPONSE.addCookie($COOKIE);
  all:
    - not:
        follows:
          stopBy: end
          pattern: $COOKIE.setValue("");
    - not:
        follows:
          stopBy: end
          pattern: $COOKIE.setHttpOnly($$$);
