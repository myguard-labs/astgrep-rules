version: 1
id: fix-format-security-error-cpp
language: cpp
category: security
severity: warning
comments:
  - 'MyGuard rule: https://github.com/myguard-labs/ast-grep-essentials | https://deb.myguard.nl'
  - 'CodeRabbit source repository: https://github.com/coderabbitai/ast-grep-essentials'
  - 'CodeRabbit source file: https://github.com/coderabbitai/ast-grep-essentials/blob/73120109bf45c284d0cd8a37bdd7082e80e92e87/rules/cpp/security/fix-format-security-error-cpp.yml'
  - 'Original author (Git): ESS-ENN'
  - 'License: Apache License 2.0 (https://www.apache.org/licenses/LICENSE-2.0)'
  - 'Modified by MyGuard: provenance metadata added; function matcher and diagnostic corrected.'
extensions:
  ast-grep-essentials: true
message: >-
  A non-literal format string can interpret attacker-controlled directives,
  causing a crash, information disclosure, or memory modification.
note: >-
  [CWE-134] Use of Externally-Controlled Format String.
rule:
  pattern: $PRINTF($S, $VAR)
constraints:
  PRINTF:
    regex: '^(sprintf|fprintf)$'
  VAR:
    not:
      any:
        - kind: string_literal
        - kind: concatenated_string
fix: $PRINTF($S, "%s", $VAR)
cases:
  valid:
    - |
      void coderabbit_control() { const char *text = "CodeRabbit"; }
    - |
      fprintf(stderr, "%s", out);
    - |
      sprintf(&buffer[2], "%s", obj->Text, a);
    - |
      sprintf(buf1, "%s", Text_String(TXT_WAITING_FOR_CONNECTIONS));
    - |
      sprintf_something(buf, value);
      myfprintf(stream, value);
  invalid:
    - |
      fprintf(stderr, out);
    - |
      sprintf(&buffer[2], obj->Text);
    - |
      sprintf(buf1, Text_String(TXT_WAITING_FOR_CONNECTIONS));
oracles:
  ? |
    fprintf(stderr, out);
  : fixed: |
      fprintf(stderr, "%s", out);
  ? |
    sprintf(&buffer[2], obj->Text);
  : fixed: |
      sprintf(&buffer[2], "%s", obj->Text);
  ? |
    sprintf(buf1, Text_String(TXT_WAITING_FOR_CONNECTIONS));
  : fixed: |
      sprintf(buf1, "%s", Text_String(TXT_WAITING_FOR_CONNECTIONS));
