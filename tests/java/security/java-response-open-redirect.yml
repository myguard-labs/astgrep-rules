id: java-response-open-redirect
valid:
  - 'response.sendRedirect("/home");'
  - 'response.sendRedirect(allowed.get(key));'
  - 'response.sendRedirect(next);'
  - 'response.setHeader("X-Next", request.getParameter("next"));'
  - 'return "redirect:/home";'
  - 'return "redirect:" + "/home";'
  - 'String note = "response.sendRedirect(request.getParameter(\"next\"))";'
  - '// response.sendRedirect(request.getParameter("next"));'
invalid:
  - 'response.sendRedirect(request.getParameter("next"));'
  - 'response.sendRedirect(base + request.getHeader("X-Next"));'
  - 'return "redirect:" + returnUrl;'
