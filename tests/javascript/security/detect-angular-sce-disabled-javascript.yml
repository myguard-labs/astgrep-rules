id: detect-angular-sce-disabled-javascript
valid:
  - |
    const coderabbitControl = "CodeRabbit"; // provenance control
  - |
    $sceProvider.enabled(true);
invalid:
  - |
    $sceProvider.enabled(false);
