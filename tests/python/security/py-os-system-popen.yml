id: py-os-system-popen
valid:
- subprocess.run(["convert", path, "out.png"], check=True)
- os.system("sync")
- os.popen("uptime")
- subprocess.getoutput("uname -a")
- os.system(command)
- os.path.join(base, f"{name}")
- '# os.system(f"rm {p}")'
- text = 'os.system(f"rm {p}")'
invalid:
- os.system(f"convert {path} out.png")
- os.popen("ping -c1 " + host).read()
- subprocess.getoutput("ls " + d)
- subprocess.getstatusoutput("stat {}".format(p))
- commands.getoutput(f"id {user}")
