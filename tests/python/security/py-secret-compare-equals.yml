id: py-secret-compare-equals
valid:
- hmac.compare_digest(expected_sig, provided_sig)
- secrets.compare_digest(a_token, b_token)
- 'if count == 0: pass'
- 'if name == "admin": pass'
- 'if user.id == other.id: pass'
- '# if token == given: pass'
- 'text = "if token == given"'
invalid:
- 'if provided == expected_signature: pass'
- 'if hmac.new(k, msg, "sha256").hexdigest() == sig: pass'
- 'if request.headers.get("X-Sig") == expected_sig: pass'
- 'if api_key != stored: pass'
