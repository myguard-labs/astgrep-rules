id: py-zipfile-extractall
valid:
- z.extract(member, dest)
- z.infolist()
- shutil.copytree(src, dst)
- shutil.make_archive(base, "zip", root)
- zipfile.ZipFile(path)
- '# z.extractall(dest)'
- text = 'z.extractall(dest)'
- tar.extractall(dest)
invalid:
- z.extractall(dest)
- archive.extractall()
- shutil.unpack_archive(upload, dest)
