id: py-pickle-equivalent-loads
valid:
- json.loads(blob)
- json.load(fh)
- marshal.dumps(obj)
- dill.dumps(obj)
- jsonpickle.encode(obj)
- shelve.Shelf(db)
- codecs.open(path)
- '# dill.loads(blob)'
- text = 'dill.loads(blob)'
invalid:
- marshal.loads(blob)
- marshal.load(fh)
- dill.loads(blob)
- dill.load(fh)
- cloudpickle.loads(blob)
- jsonpickle.decode(data)
- shelve.open(user_path)
