id: py-pickle-loads
valid:
- json.loads(data)
- pickle.dumps(data)
- notpickle.loads(data)
- '# pickle.loads(data)'
- text = 'pickle.loads(data)'
invalid:
- pickle.loads(data)
- pickle.load(stream)
- pickle . loads(data)
